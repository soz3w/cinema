{% extends "::base.html.twig" %}
 {% block stylesheets %}
     <link href="http://fonts.googleapis.com/css?family=Open+Sans:300italic,400italic,600italic,700italic,400,600,700,300&subset=latin" rel="stylesheet" type="text/css">

     <!-- Pixel Admin's stylesheets -->
     <link href="{{ asset("bo/stylesheets/bootstrap.min.css") }}" rel="stylesheet" type="text/css">
     <link href="{{ asset("bo/stylesheets/pixel-admin.min.css") }}" rel="stylesheet" type="text/css">
     <link href="{{ asset("bo/stylesheets/widgets.min.css") }}" rel="stylesheet" type="text/css">
     <link href="{{ asset("bo/stylesheets/rtl.min.css") }}" rel="stylesheet" type="text/css">
     <link href="{{ asset("bo/stylesheets/themes.min.css") }}" rel="stylesheet" type="text/css">
     <link href="{{ asset("bo/stylesheets/cinema.css") }}" rel="stylesheet" type="text/css">
 {% endblock %}

{% block body %}
    <script>var init = [];</script>
    <!-- Demo script --> <script src="{{ asset("bo/demo/demo.js") }}"></script> <!-- / Demo script -->
    <div class="container">
        {% block menu %}
            <nav class="navbar navbar-inverse navbar-fixed-top">
                <div class="container-fluid">
                    <ul class="nav navbar-nav">
                        <li> <a class ="accueil" href="{{ path('cinema_bo')}}">Accueil</a> </li>
                        <li> <a class ="login" href="{{ path('cinema_bo_movies')}}">Films</a> </li>
                        <li> <a class ="register" href="{{ path('cinema_bo_categories')}}" >Categories</a> </li>
                        <li> <a class ="contact" href="{{ path('cinema_bo_Actors')}}">Acteurs</a> </li>
                        <li> <a class ="contact" href="{{ path('cinema_bo_Directors')}}">Realisateurs</a> </li>
                        <li> <a class ="contact" href="{{ path('cinema_bo_Seances')}}">Séances</a> </li>
                        <li> <a class ="contact" href="{{ path('cinema_bo_Cinemas')}}">Cinémas</a> </li>

                    </ul>

                </div>
            </nav>
        {% endblock %}
        {% block sidebar %}
            <div class="col-sm-4">
                {{ include("CinemaBoBundle:slots:sidebar.html.twig") }}

            </div>
        {% endblock %}
        {% block content %}

        {% endblock %}

            {%  use("CinemaBoBundle:slots:footer.html.twig") %}
            {% block footer %}
                {{ parent() }}
            {% endblock %}
        {% block javascripts %}
            <script src="//ajax.googleapis.com/ajax/libs/jquery/2.1.3/jquery.min.js"></script>
            <script src="{{ asset("bo/javascripts/bootstrap.min.js")  }}"></script>
            <script src="{{ asset("bo/javascripts/pixel-admin.min.js") }}"></script>
        {% endblock %}
    </div>
{% endblock %}