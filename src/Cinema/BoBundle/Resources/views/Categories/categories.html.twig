{% extends "CinemaBoBundle::layout.html.twig" %}
{% block content %}
    <div class="col-md-8">
        {% for category in categories %}
            <li><a  href="{{ path('cinema_bo_movies_category',
                {'catId':category.id})}}">{{category.title}}</a>

            </li>
        {% endfor %}
    </div>
{% endblock %}