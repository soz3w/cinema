{% for tag in tags %}

        <li class="list-group-item">
            <a href="{{ path('cinema_bo_movies_tag',
            {'tagId':tag.id})}}">{{tag.word}}</a>
            <span class="pull-right badge">{{ tag.nbMovies }}</span>
        </li>

{% endfor %}